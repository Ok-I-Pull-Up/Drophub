{
	"version": 2,
	"builds": [
		{
			"src": "package.json",
			"use": "@vercel/static-build",
			"config": {
				"installCommand": "npm cache clean --force && npm i -g npm@latest && npm install --no-package-lock --force ajv@8.12.0 ajv-keywords@5.1.0 && npm install --legacy-peer-deps",
				"buildCommand": "npm run build"
			}
		}
	],
	"env": {
		"CI": "false",
		"ESLINT_NO_DEV_ERRORS": "true",
		"DISABLE_ESLINT_PLUGIN": "true"
	}
}
