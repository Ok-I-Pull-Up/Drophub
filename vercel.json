{
	"version": 2,
	"builds": [
		{
			"src": "package.json",
			"use": "@vercel/static-build",
			"config": {
				"installCommand": "npm cache clean --force && npm install --no-package-lock --legacy-peer-deps",
				"buildCommand": "npm run build"
			}
		}
	]
}
